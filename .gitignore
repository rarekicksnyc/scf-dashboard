node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.DS_Store
# Never commit secrets (DATABASE_URL, SESSION_SECRET, etc.)
.env
.env.*
!.env.example

# CI workflow file is added under .github/ via the GitHub UI (needs workflow scope)
.github/workflows/ci.yml
